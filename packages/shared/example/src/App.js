import React, { Component } from 'react'

import {Button} from '@kglowiak/shared'

export default class App extends Component {
  render () {
    return (
      <div>
        Later it can be replaced with storybook
        <h2>Button component</h2>
        <Button>test text</Button>
      </div>
    )
  }
}
